# Frontend Environment Variables
# Copy this file to .env.local and fill in the values

# Backend canister ID (required for the app to function)
# This should be set automatically during deployment via env.json,
# but can be provided here as a fallback.
# 
# Get your canister ID by running: dfx canister id backend
VITE_BACKEND_CANISTER_ID=

# Alternative canister ID variable name (for compatibility)
VITE_CANISTER_ID_BACKEND=

# Network host (optional)
# Leave empty for mainnet (production)
# Use 'local' or 'http://localhost:4943' for local development
# VITE_HOST=
# VITE_DFX_NETWORK=local

# Internet Identity derivation origin (optional)
# Only needed if using a custom Internet Identity instance
# VITE_II_DERIVATION_ORIGIN=

# ⚠️ IMPORTANT: Do not paste broken/old canister IDs here
# Always use the canister ID from your latest successful deployment
# Run: dfx canister id backend
